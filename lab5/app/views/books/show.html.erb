<h2><%= @book.title %></h2>
<p><strong>Authors:</strong> <%= @book.authors.map(&:name).join(', ') %></p>
<p><strong>Genres:</strong> <%= @book.genres.map(&:name).join(', ') %></p>
<%= link_to 'Edit', edit_book_path(@book), class: 'btn btn-warning' %>
<%= link_to 'Go back', books_path, class: 'btn btn-secondary' %>
